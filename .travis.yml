sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t gjohnsonatclearwater/docker-react -f DockerFile.dev .

script:
  - docker run -e CI=true gjohnsonatclearwater/docker-react npm run test